<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || shop.shop_name || 'Book A Barber' %></title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="bg-orb orb-a"></div>
  <div class="bg-orb orb-b"></div>
  <header class="topbar">
    <a class="brand" href="/"><%= shop.shop_name || 'Book A Barber' %></a>
    <nav>
      <a href="/">Book</a>
      <a href="/barber/login">Barber</a>
      <a href="/admin/login">Admin</a>
      <% if (admin) { %><a href="/admin/logout">Logout</a><% } %>
      <% if (barberSessionId) { %><a href="/barber/logout">Logout</a><% } %>
    </nav>
  </header>
  <main class="shell">
